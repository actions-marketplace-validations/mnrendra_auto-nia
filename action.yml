name: 'Auto Nia'
description: 'Auto NIA.'

branding:
  icon: 'play'
  color: 'green'

inputs:
  text:
    description: 'Any text.'
    default: 'Nia'

runs:
  using: "composite"
  steps:
    - name: 'Print'
      run: echo "Hai ${{ inputs.text }}"
      shell: bash

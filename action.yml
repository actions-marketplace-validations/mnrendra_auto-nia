name: auto-nia
description: Auto NIA
author: mnrendra
branding:
  icon: 'play'
  color: 'purple'

inputs:
  text:
    description: Any text.
    default: 'Nia'

runs:
  steps:
    - name: echo
      run: echo "Hai ${{ inputs.text }}"
      shell: bash
